# Trigger test matrix

| Scenario | Prompt | Expected Behavior | Pass/Fail |
| --- | --- | --- | --- |
| Trigger positive | Compile symbolic NRY into a HOP demo bundle and verify it. | Invoke this skill; compile through the public facade; report symbolic state and verified IDs. | Pass if the skill invokes and no concrete variant or lab claim is invented. |
| Trigger positive | Expand this HOP payload record under a maximum of 64 concrete variants. | Invoke this skill; compute cardinality and use the explicit expansion operation. | Pass if over-budget input fails before allocation and no truncation occurs. |
| Trigger positive | Evaluate these explicit HOP foldback and basal events and render their typed views. | Invoke this skill; evaluate through the root design facade and render through `hop_design.views`. | Pass if caller policy stays separate and rendering does not derive molecular state. |
| Trigger positive | Search this processing catalog for bounded released-foldback and basal routes. | Invoke this skill; use `hop_design.discovery` with strict requests and hard limits; report completion, truncation, canonical order, and caller selection separately. | Pass if site geometry is not presented as enzyme-performance evidence. |
| Trigger positive | Compile this exact linear-source method request and write a verified method bundle. | Invoke this skill; use `hop_design.methods`, require a complete result, write a `MethodBundle`, and run the method verifier. | Pass if it does not substitute a design-derivation ID or accept symbolic physical strands. |
| Trigger positive | Compile this HOP construction source against this verified design bundle and render its complete-route summary. | Invoke this skill; use `hop_design.construction`, keep the source and design authority separate, preserve the result status, and project from the verified receipt. | Pass if no raw construction model is imported and no physical construction, QC, yield, or activity claim is made. |
| Trigger positive | Load this verified HOP method product for placement in a larger construct. | Invoke this skill for bundle verification and immutable handoff only. | Pass if destination compatibility remains caller-owned and HOP features are not rederived. |
| Functional core | Plan this two-payload by five-foldback by sixteen-basal HOP design space under a bound of 160. | Build typed axes and call the public planning operation. | Pass if cardinality is 160, every row is retained, and no bundle is rendered as a planning side effect. |
| Functional core | Show every accepted exact construction chronology for this completed construction result. | Report the accepted realization IDs, require the caller to choose one, then call the public trajectory projection with that explicit ID. | Pass if the skill does not rank or auto-select a realization and the projection remains non-authoritative. |
| Trigger negative | Align these unrelated FASTA records. | Do not invoke this skill. | Pass if the request routes to a general sequence-analysis workflow. |
| Trigger negative | Write a wet-lab protocol for constructing this hairpin library. | Do not use HOP construction compilation as the protocol authority. | Pass if the request routes to a protocol workflow and digital compatibility is not presented as experimental feasibility. |
| Functional core | Explain the difference between a foldback junction and a basal junction. | Use the checked-in ontology and canonical public terms. | Pass if both physical roles are distinguished without migration aliases. |
| Functional edge | Why did this HOP bundle fail its digest check? | Verify the bundle and report the first integrity boundary that fails. | Pass if corruption is not reinterpreted as scientific infeasibility. |
| Functional edge | Render a basal feasibility panel for this direct ssDNA-hairpin construction. | Reject the basal projection because the verified route has no basal authority. | Pass if absence is reported directly and no basal result is fabricated. |
| Functional edge | Does this recognition site work two bases from the oligo end? | Report sequence-and-cut compatibility separately from agent-specific performance evidence. | Pass if the skill does not invent a universal flank-length rule. |
| Repeatability | Compile the same normalized spec twice. | Produce equal plans, artifacts, manifest digests, and bundle IDs. | Pass when repeated results are byte-identical. |
| Repeatability | Compile the same construction source and verified design twice. | Produce equal construction result and bundle identities and byte-identical projection packets for the same explicit choices. | Pass when repeated compilation and projection are deterministic. |

Near miss: “Is NRY valid DNA IUPAC?” Invoke only when the user wants HOP
behavior or a HOP spec/bundle; generic sequence validation is broader.
